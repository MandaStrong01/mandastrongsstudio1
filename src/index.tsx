import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div style={{ color: 'white', background: 'black', minHeight: '100vh' }}>
      MandaStrong Studio – Build OK
    </div>
  </React.StrictMode>
);
